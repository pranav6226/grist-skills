# Grist agent skills

Install:

```bash
npx skills add grist-ai/grist-skills
```

That loads the Grist skill into Cursor, Claude, OpenCode, Muse, and other agents the CLI detects.

The API key stays in a vault as `GRIST_API_KEY`. Never in this repo, never in chat.
